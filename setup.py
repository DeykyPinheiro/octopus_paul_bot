from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a bot that analyzes a time series of assets and returns the action on the day and inserts the result into the database',
    author='Your name (or your organization/company/team)',
    license='',
)
